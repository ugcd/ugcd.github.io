---
layout: page
title: Publications of the group
---

```{r setup, include = FALSE}
opts_chunk$set(echo = FALSE, cache = FALSE, tidy = FALSE, warning = FALSE, message = FALSE)
opts_knit$set(upload.fun = identity)
```

```{r}
library(knitcitations)
```

```{r knitcitations}
#cite_options(linked = TRUE)
#cite_options(tooltip = TRUE)
cite_options(max.names = 20, hyperlink = TRUE, style = "markdown")
```

```{r}
print_bib <- function()
{
  bibliography(sorting = 'ydnt', max.names = 20, no.print.fields = c("url")) # sort by year, descending, then name, title
}
```

## Articles

```{r}
articles <- read.bibtex("bib/articles.bib")
```

```{r, results = "hide"}
for(i in 1:length(articles)) {
  citet(articles[[i]])
} 
```

```{r, results = "asis"}
print_bib()
cleanbib()
```

## Conferences

```{r}
conferences <- read.bibtex("bib/conferences.bib")
```

```{r, results = "hide"}
for(i in 1:length(conferences)) {
  citet(conferences[[i]])
} 
```

```{r, results = "asis"}
print_bib()
cleanbib()
```

